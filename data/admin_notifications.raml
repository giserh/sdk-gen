displayName: Notifications
type: common
post:
  description: Add a notification.
  body:
    application/json:
      schema: !include schemas/admin_notification_schema.json
get:
  is: [fields, paginator, ordering]
  description: Get a list of notifications.
  responses:
    200:
      body:
        schema: !include schemas/admin_notification_schema.json
/doc:
  displayName: NotificationsDocs
  type: common
  get:
    description: Get a pseudo schema showing quickly how to make a proper json for notifications.
/{id}:
  displayName: OneNotification
  type: common
  get:
    is: [fields]
    description: Get one notification.
    responses:
      200:
        body:
          schema: !include schemas/admin_notification_schema.json
  put:
    description: Update a notification.
    body:
      application/json:
        schema: !include schemas/admin_notification_schema.json
  delete:
    description: Delete a notification.
/types:
  displayName: NotificationTypes
  type: common
  post:
    description: Add a notification type.
    body:
      application/json:
        schema: !include schemas/admin_notification_type_schema.json
  get:
    is: [fields, paginator, ordering]
    description: Get a list of notification types.
    responses:
      200:
        body:
          schema: !include schemas/admin_notification_type_schema.json
  /doc:
    displayName: NotificationTypesDocs
    type: common
    get:
      description: Get a pseudo schema showing quickly how to make a proper json for notification types.
  /{id}:
    displayName: OneNotificationType
    type: common
    get:
      is: [fields]
      description: Get a notification type.
      responses:
        200:
          body:
            schema: !include schemas/admin_notification_type_schema.json
    put:
      description: Update a notification type
      body:
        application/json:
          schema: !include schemas/admin_notification_type_schema.json
    delete:
      description: Delete a notification type.