displayName: Segments
type: common
post:
  description: Add a new segment.
  body:
    application/json:
      schema: !include schemas/admin_segment_schema.json
get:
  is: [fields, paginator, ordering]
  description: Get a segment list.
  responses:
    200:
      body:
        schema: !include schemas/admin_segment_schema.json
/doc:
  displayName: SegmentsDocs
  type: common
  get:
    description: Get a pseudo schema showing quickly how to make a proper json for segment.
/{id}:
  displayName: Segment
  type: common
  get:
    is: [fields]
    description: Get a segment.
    responses:
      200:
        body:
          schema: !include schemas/admin_segment_schema.json
  put:
    description: Update a segment.
    body:
      application/json:
        schema: !include schemas/admin_segment_schema.json
  delete:
    description: Delete a segment.
